# Sequential Orchestration (Legacy)

This file is kept for backward compatibility with older scripts.

The canonical workflow is now driven by:

```bash
python scripts/sequential_loop.py --spec .kiro/specs/<spec-name>
```

See `SKILL.md` for details.
